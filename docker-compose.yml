
services:
  bot-gateway:
    build:
      context: ./bot-gateway
    container_name: bot-gateway
    environment:
      - SERVER_URL=https://gateway.squadfinders.ir
      - PORT=3000 # inside port
      - PROXY_PASS=true # proxy pass with nginx
    restart: unless-stopped
    networks:
      - nginx

networks:
  nginx: